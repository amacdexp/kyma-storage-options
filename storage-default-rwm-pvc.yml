apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: default-rwm-pvc
  #labels:
  #  demo: nfs-pv-provisioning
spec:
  accessModes: [ "ReadWriteMany" ]
  resources:
    requests:
      storage: 10Gi